# /write — Phase 2: 執筆フェーズを実行

`draft.md` と `research_cache/` の最新ファイルを読み込み、ブログ記事の下書きを `articles/` に生成します。

## 前提確認

実行前に `research_cache/` ディレクトリにファイルが存在することを確認してください。
ファイルがない場合は `/research` を先に実行してください。

## 実行手順

以下の手順で実行してください:

1. **subagent "writer-agent"** を起動する
2. エージェントが完了したら、生成された `articles/` ファイルのパスをユーザーに報告する

## 完了後のメッセージ

エージェント完了後、以下を表示してください:

```
✍️  執筆完了（下書き）
ファイル: articles/{YYYYMMDD}_{相手チーム名}.md

次のステップ:
  - articles/ の下書きを確認する
  - 問題なければ /edit を実行
  - 表現・構成の修正は直接ファイルを編集 → /edit を実行
  - 方向性の修正が必要な場合はチャットで指示 → /write を再実行
```
